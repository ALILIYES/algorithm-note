#include<cstdio>
int module(int a[],int low,int high,int N){
	int mid;
	while(low+1<high){
		mid=(low+high)/2;
		if(条件成立){
			high=mid; 
		} 
		else{
			low=mid;
		}
	}
	return right;
} 
